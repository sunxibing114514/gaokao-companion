<Window x:Class="GaokaoCompanion.Windows.NowPlayingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="正在播放"
        Width="310"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Topmost="True"
        ResizeMode="NoResize">
    <Border x:Name="Root"
            CornerRadius="16"
            Background="#E6141218"
            BorderBrush="#26FFFFFF"
            BorderThickness="1"
            Padding="16,12"
            Margin="10"
            MouseLeftButtonDown="OnDismiss">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="18" ShadowDepth="3" Opacity="0.45"/>
        </Border.Effect>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="♪" Foreground="{StaticResource Primary}" FontSize="14"
                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Text="正在播放" Foreground="{StaticResource OnSurfaceVariant}" FontSize="12"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock x:Name="TitleText" Text=""
                       Foreground="{StaticResource OnSurface}" FontSize="14" FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis" Margin="1,5,0,0" MaxWidth="266"/>
            <TextBlock x:Name="HintText" Text="点击关闭 · Ctrl+Alt+X 停止"
                       Foreground="{StaticResource OnSurfaceVariant}" FontSize="11" Margin="1,3,0,0"/>
        </StackPanel>
    </Border>
</Window>
